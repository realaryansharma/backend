npm -  doesn't have an actual full form; you can check there actual website.
npm packages can be installed by - npm i or npm install {packageName}
to install package globally npm i nodemon -g
use require to import the package